# === MINIO + CONSOLE (via Traefik) ===
server {
    listen 80;
    server_name minio.seudominio.com console.seudominio.com;

    # Redireciona HTTP → HTTPS
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    server_name minio.seudominio.com;

    # Seu certificado Let's Encrypt atual
    ssl_certificate /etc/letsencrypt/live/minio.seudominio.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/minio.seudominio.com/privkey.pem;

    # Proxy para o Traefik (HTTPS interno)
    location / {
        proxy_pass https://127.0.0.1:444;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Importante para MinIO (S3)
        proxy_buffering off;
        proxy_request_buffering off;
        chunked_transfer_encoding on;
    }
}

server {
    listen 443 ssl http2;
    server_name console.seudominio.com;

    ssl_certificate /etc/letsencrypt/live/console.seudominio.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/console.seudominio.com/privkey.pem;

    location / {
        proxy_pass https://127.0.0.1:444;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}


====

para proxy radio

# Exemplo: Streaming HLS / RTMP
server {
    listen 443 ssl http2;
    server_name stream.seudominio.com;

    ssl_certificate ...;
    ssl_certificate_key ...;

    # Configurações de streaming aqui (location /hls/, /dash/, etc.)
    location /hls/ {
        proxy_pass http://127.0.0.1:PORT_DO_SEU_STREAMING;
        # ... suas configs de streaming
    }
}