#!/usr/bin/env bash
# exemplo: ps-user-delete USERNAME

set -euo pipefail

# --------------------------
# Variáveis
# --------------------------
USERNAME="${1:-}"

# --------------------------
# Funções
# --------------------------
erro() { echo "{\"status\":\"error\",\"message\":\"$1\"}" >&2; exit 1; }
sucesso() { echo "{\"status\":\"success\",\"message\":\"$1\",\"data\":{\"user\":\"$USERNAME\"}}"; exit 0; }

# --------------------------
# Validações
# --------------------------
[[ -z "$USERNAME" ]] && erro "usuario obrigatorio"
[[ ! "$USERNAME" =~ ^[a-z]+$ ]] && erro "usuario invalido"
id "$USERNAME" &>/dev/null || erro "usuario '$USERNAME' nao existe"

# --------------------------
# Includes e paths
# --------------------------
PAINELSTREAM="/usr/local/painelstream"

# --------------------------
# Mata processos do usuário
# --------------------------
pkill -u "$USERNAME" 2>/dev/null || true

# --------------------------
# Remove usuário e diretórios
# --------------------------
# Remove o usuário do sistema
sudo userdel "$USERNAME" || erro "falha ao remover usuario"

# Remove manualmente o home se existir
[[ -d "/home/$USERNAME" ]] && sudo rm -rf "/home/$USERNAME"

# --------------------------
# Rebuild Icecast
# --------------------------
sudo "$PAINELSTREAM/src/icecast-build.sh" || true

# --------------------------
# Resposta de sucesso
# --------------------------
sucesso "Radio removida com sucesso"