#!/bin/bash
set -e

USERNAME="$1"
PASS="$2"

# Includes
source "/usr/local/painelstream/func/main.sh"

validar_usuario_pass "$USERNAME" "$PASS"

echo "$USERNAME:$PASS" | sudo chpasswd

sucesso "Senha alterada com sucesso" "{\"user\":\"$USERNAME\"}"
